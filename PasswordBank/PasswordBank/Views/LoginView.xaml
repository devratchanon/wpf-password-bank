<UserControl x:Class="PasswordBank.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             
             xmlns:local="clr-namespace:PasswordBank.Views"
             
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="600">
    <Grid>
        <Border Background="Gray"
                Height="370" Width="370"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
            <StackPanel>
                <TextBlock FontSize="20"
                           Margin="20">Username:</TextBlock>
                <TextBox Margin="20, 0"></TextBox>
                
                <TextBlock FontSize="20"
                           Margin="20">Password:</TextBlock>
                <PasswordBox Margin="20, 0"></PasswordBox>
                
                <Button Command="{Binding LoginCommand}"
                        Margin="20, 40, 20, 0">Login</Button>
                <Button Margin="20">New User</Button>
                
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
